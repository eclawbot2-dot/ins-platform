/**
 * Alias route for the marketing funnel — /coverage-checkup is the
 * customer-facing name the website CTA points at; it renders the same
 * public Compare landing as /compare.
 */
export { metadata } from "@/app/compare/page";
export { default } from "@/app/compare/page";
